= form_for(@user, :url => account_path) do |f|
  %div
    %h3 Subscribe to Geofence, High Speed, Off-hour Use, and Tow alerts.
  %div
    %div
      %b Email addresses:
    %div
      %input{disabled: "disabled", type: "text", value: current_user.email}/
      #emailsInput{style: 'display:none'}= f.text_field :notification_emails
    %div= f.submit :save